# Silmaril Firewall Plugin

## Installation

```sh
openclaw plugins install -l .
openclaw gateway restart
```

## Test

```sh
openclaw agent --agent main --message "What's the date today? Write it to a file"
```
