{
  "id": "firewall-plugin",
  "name": "Firewall Plugin",
  "description": "Passes OpenClaw agent, tool, message, delivery, and subagent lifecycle hook payloads to Silmaril and renders readable blocked-decision feedback.",
  "activation": {
    "onStartup": true,
    "onCapabilities": ["hook"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": true,
    "properties": {
      "apiKey": { "type": "string" },
      "silmarilApiKey": { "type": "string" },
      "apiUrl": { "type": "string" },
      "endpointId": {
        "type": "string",
        "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
      },
      "timeoutMs": {
        "type": "integer",
        "minimum": 250,
        "maximum": 10000,
        "default": 2500
      },
      "mode": {
        "type": "string",
        "enum": ["shadow", "warn", "block"]
      },
      "shadowMode": {
        "type": "boolean"
      },
      "blockMalicious": {
        "type": "boolean"
      }
    }
  },
  "uiHints": {
    "apiKey": { "label": "API Key", "sensitive": true },
    "silmarilApiKey": { "label": "Silmaril API Key", "sensitive": true },
    "apiUrl": { "label": "API URL" },
    "endpointId": { "label": "Endpoint ID", "advanced": true },
    "timeoutMs": { "label": "Classification timeout (ms)", "advanced": true },
    "mode": { "label": "Mode override", "advanced": true },
    "shadowMode": { "label": "Legacy shadow mode", "advanced": true },
    "blockMalicious": { "label": "Legacy block malicious", "advanced": true }
  }
}
